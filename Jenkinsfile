pipeline {
  agent any

  stages {

    stage('Build') {
      steps {
        echo 'No build needed for static site'
      }
    }

    stage('Test') {
      steps {
        echo 'No tests yet'
      }
    }

    stage('Deploy') {
      steps {
        echo 'Deploying to GitHub Pages...'
      }
    }
  }
}
